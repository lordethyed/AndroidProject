<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/app_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/app_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.Final.NoActionBar"
        tools:targetApi="31">

        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <activity
            android:name=".test"
            android:exported="false" />

        <activity
            android:name=".test_ana"
            android:exported="false" />

        <activity
            android:name=".test_apa"
            android:exported="false" />

        <activity
            android:name=".test_ini"
            android:exported="false" />

        <activity
            android:name=".test_karindas"
            android:exported="false" />



        <activity
            android:name=".authentification.loginActivity"
            android:exported="false"
            android:theme="@style/Theme.Final"

            />

        <activity
            android:name=".authentification.registrationActivity"
            android:exported="false" />

        <activity
            android:name=".splash.SplashScreen"
            android:exported="true"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>